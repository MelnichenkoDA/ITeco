#ifndef __ALGO__HPP
#define __ALGO__HPP

#include <fstream>
#include <iostream>
#include <map>
#include <limits>


using std::cout;
using std::ifstream;
using std::ofstream;
using std::map;

std::string func(const int lim, const char * inputFilename, const char * outputFilename);

#endif